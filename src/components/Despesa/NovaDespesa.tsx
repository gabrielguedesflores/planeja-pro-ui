import { Button } from "..";
import { Colors } from "../../assets/theme";

export default function FiltroDespesa() {
  return (
    <Button text="Adicionar" buttonColor={Colors.primary} variant="contained" />
  );
}